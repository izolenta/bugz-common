class ObjectPair<K, T> {
  final K first;
  final T second;

  ObjectPair(this.first, this.second);
}

class ObjectThree<K, T, M> {
  final K first;
  final T second;
  final M third;

  ObjectThree(this.first, this.second, this.third);
}

